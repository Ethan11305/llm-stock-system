[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["python3 -m pip install -e '.[dev]'"]

[start]
cmd = "uvicorn llm_stock_system.api.app:create_app --factory --host 0.0.0.0 --port $PORT"
