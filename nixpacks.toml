[phases.install]
cmds = ["python -m venv --copies /opt/venv && /opt/venv/bin/pip install -e '.[dev]'"]

[start]
cmd = "/opt/venv/bin/uvicorn llm_stock_system.api.app:create_app --factory --host 0.0.0.0 --port $PORT"
